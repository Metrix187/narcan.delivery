// Copy this file to secrets.h and fill in. secrets.h is gitignored.
#pragma once

// --- WiFi (2.4 GHz; the C6 is single-band) -------------------------------
#define WIFI_SSID      "your-network"
#define WIFI_PASSWORD  "your-password"

// --- Source of truth -----------------------------------------------------
// Same published CSV the site build + snapshot-sheet.mjs use. Keep them equal.
#define SHEET_CSV_URL  "https://docs.google.com/spreadsheets/d/e/2PACX-1vQo7lo4oD6rLjt4NWISOR3AJy1AtPOA3com6ntoo6LX_lSV8dmylHDjLcj3KIklR44peURjksWPo86R/pub?gid=0&single=true&output=csv"

// --- Optional failover trigger -------------------------------------------
// Cloudflare Pages "Deploy hook" URL. When the guardian declares the sheet
// DEAD, it POSTs this once to rebuild the live site -- which falls back to the
// git backup mirror. Leave empty to disable.
#define DEPLOY_HOOK_URL ""
